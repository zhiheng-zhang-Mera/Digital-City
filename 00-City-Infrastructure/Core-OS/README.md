# Core OS

STATUS = STRUCTURAL_SLOT
PROJECT_MAPPING = PENDING_REVIEW

City-wide runtime/substrate slot. The exact repository mapping and future naming are intentionally deferred to the project-content review.

This layer may own generic runtime, identity/trust, routing, lifecycle and city-wide authority primitives, but must not absorb domain business logic.
